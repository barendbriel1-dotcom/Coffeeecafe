# eCafe

Fresh Vite, React, TypeScript, Tailwind, shadcn/ui, and Supabase app foundation.

## Local Development

```bash
npm install
npm run dev
```

Create a `.env` file from your new Supabase project:

```bash
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
```
